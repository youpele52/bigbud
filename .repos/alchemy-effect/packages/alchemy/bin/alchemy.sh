#!/bin/sh
# Auto-generated by scripts/hoist-alchemy-bin.ts. Do not edit.
# Dev-only shim: runs alchemy.ts under bun, falls back to alchemy.js under node.
RUNTIME=node
case "$npm_execpath" in
  *bun*) RUNTIME=bun ;;
esac
if [ "$RUNTIME" = "node" ] && command -v bun >/dev/null 2>&1; then
  RUNTIME=bun
fi
if [ "$RUNTIME" = "bun" ]; then
  exec bun "/Users/samgoodwin/workspaces/alchemy-effect/packages/alchemy/bin/alchemy.ts" "$@"
else
  exec node "/Users/samgoodwin/workspaces/alchemy-effect/packages/alchemy/bin/alchemy.js" "$@"
fi
