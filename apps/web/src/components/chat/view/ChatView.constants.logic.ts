/** Columns used when opening a new script terminal. */
export const SCRIPT_TERMINAL_COLS = 120;
/** Rows used when opening a new script terminal. */
export const SCRIPT_TERMINAL_ROWS = 30;

/** Debounce wait in ms for the composer path query. */
export const COMPOSER_PATH_QUERY_DEBOUNCE_MS = 120;
