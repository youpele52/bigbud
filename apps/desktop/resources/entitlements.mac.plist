<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
  <dict>
    <!-- Required for App Sandbox -->
    <key>com.apple.security.app-sandbox</key>
    <true/>
    <!-- Allow outbound network connections (WebSocket to OpenAI, update checks, etc.) -->
    <key>com.apple.security.network.client</key>
    <true/>
    <!-- Allow microphone access for STT voice transcription -->
    <key>com.apple.security.device.audio-input</key>
    <true/>
    <!-- Allow JIT compilation (required by Electron's V8 engine) -->
    <key>com.apple.security.cs.allow-jit</key>
    <true/>
    <!-- Allow unsigned executable memory (required by Electron) -->
    <key>com.apple.security.cs.allow-unsigned-executable-memory</key>
    <true/>
    <!-- Allow dyld environment variables (required by Electron) -->
    <key>com.apple.security.cs.allow-dyld-environment-variables</key>
    <true/>
  </dict>
</plist>
