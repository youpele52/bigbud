# cua-driver 0.9.1 contract

Captured by `bun run scripts/verify-cua-driver-contract.ts` on darwin/arm64. The harness uses production release metadata, child environment, invocation arguments, and policy bytes.
